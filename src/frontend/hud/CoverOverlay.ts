/**
 * CoverOverlay (Phase 4)
 * Semi-transparent front-cover [Play] button overlay for instant random auditioning.
 */
export class CoverOverlay {
  public render(container: HTMLElement, onPlayClick: () => void): void {
    console.log("[CoverOverlay] Rendering play button HUD overlay.");
  }
}
